/*PROGRAM TO ADD,SUBSTRACT AND TRANSPOSE OF MATRIX*/

#include<stdio.h>
#include<conio.h>
void main()
{
int a[10][10],b[10][10],c[10][10],x,y,p,q,i,j,ch,temp;
clrscr();
printf("\nenter no. of rows in matrix A:");
scanf("%d",&x);
printf("\nenter no. of columns in matrix A:");
scanf("%d",&y);
printf("\nenter matrix A:");
for(i=0;i<x;i++)
for(j=0;j<y;j++)
scanf("%d",&a[i][j]);
printf("\nmatrix A is:");
for(i=0;i<x;i++)
{
printf("\n");
for(j=0;j<y;j++)
{
printf("  %d",a[i][j]);
}
}
printf("\n\n\n\n\nenter no. of rows in matrix B:");
scanf("%d",&p);
printf("\nenter no. of columns in matrix B:");
scanf("%d",&q);
printf("\nenter matrix B:");
for(i=0;i<p;i++)
for(j=0;j<q;j++)
scanf("%d",&b[i][j]);
printf("\nmatrix B is:");
for(i=0;i<p;i++)
{
printf("\n");
for(j=0;j<q;j++)
{
printf("  %d",b[i][j]);}}
printf("\n\n\nchoice:\naddition-1\nsubtraction-2\ntranspose-3\n");
printf("\n\n\nenter your choice:");
scanf("%d",&ch);
if((ch==1)||(ch==2))
{
if((ch==1)&&(x==p)&&(y==q))
{
printf("\nmatrix can be added");
for(i=0;i<p;i++)
for(j=0;j<q;j++)
c[i][j]=a[i][j]+b[i][j];
printf("\n\nmatrix A+B is:");
for(i=0;i<p;i++)
{
printf("\n");
for(j=0;j<q;j++)
{
printf("  %d",c[i][j]);
}
}
}
if((ch==2)&&(x==p)&&(y==q))
{
printf("\n\nmatrix can be subtracted");
for(i=0;i<p;i++)
for(j=0;j<q;j++)
c[i][j]=a[i][j]-b[i][j];
printf("\n\nmatrix A-b is:");
for(i=0;i<p;i++)
{
printf("\n");
for(j=0;j<q;j++)
{
printf("  %d",c[i][j]);}}
}
}
if(ch==3)
{
for(i=0;i<x;i++)
{
for(j=i+1;j<y;j++)
{
temp=a[i][j];
a[i][j]=a[j][i];
a[j][i]=temp;
}
}
printf("\n\nA transpose is:");
for(i=0;i<x;i++)
{
printf("\n");
for(j=0;j<y;j++)
{
printf("  %d",a[i][j]);
}
}
for(i=0;i<p;i++)
{
for(j=i+1;j<q;j++)
{
temp=b[i][j];
b[i][j]=b[j][i];
b[j][i]=temp;
}
}
printf("\n\nB transpose is:");
for(i=0;i<p;i++)
{
printf("\n");
for(j=0;j<q;j++)
{
printf("  %d",b[i][j]);
}
}
}
getch();
}