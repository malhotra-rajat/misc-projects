# Gadget Guru

ASP.NET Web Forms training-project snapshot: gadgets, product pages, registration, login and secured pages. Preserved from the `.NET Training/Project` folder. Original/group authorship is not independently established.

Not run. Uses SQL Express with an attached `Database.mdf`; the database and log were excluded. Reconstruct clean `UserDetails`/product tables from the source queries and page bindings, update both configured and hardcoded connection strings, and run locally with a Windows Web Forms development environment. Review field types/constraints against all callers before claiming a complete schema. See [RUNNING.md](../../RUNNING.md).
